spring:
  thymeleaf:
    mode: HTML
    encoding: UTF-8
  mvc:
    date-format: yyyy-MM-dd
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/demo1?useUnicode=true&serverTimezone=UTC&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto:  none # update: 项目启动时，更新数据库字段
    show-sql: true

server:
  port: 8080
